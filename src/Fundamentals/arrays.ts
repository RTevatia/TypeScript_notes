// let numbers = [1, 2, 3];
// let numbers: number[] = [1, 2, 3];
// let numbers: number[] = [1, 2, '3'];

let numbers: number[] = [];
numbers[0] = 1;
numbers[1] = 10;
// numbers[2] = 'abcd';

numbers.forEach((n) => n.toFixed(2));
